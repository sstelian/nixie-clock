The code was written for Arduino 1.0.4 . It might not compile with other versions. The code depends on the RTClib library which is not included.

The electronics design files include just the main board and the driver board.

*Although the project is quite messy, it has been working for a few years continuously. 
